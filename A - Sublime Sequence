#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

int main()
{
    int t;
    cin >> t;

    while(t--)
    {
        int x, n;
        cin >> x >> n;

        int sum;

        if(n % 2 == 0) sum = 0;
        else sum = x;

        cout << sum <<endl;
    }
}
