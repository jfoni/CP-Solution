#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

int main()
{
    string n;
    getline(cin,n);

    set<char> s;
    for(int i=1;i<n.size()-1;i++){
        if(n[i]>='a' && n[i]<='z'){
            s.insert(n[i]);
        }
    }

    cout << s.size() <<endl;
}
