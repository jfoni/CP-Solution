#include<bits/stdc++.h>
using namespace std;
 
int main()
{
    vector<int>v(4);
    for(int i=0;i<4;i++)
    {
        cin >> v[i];
    }
 
    int m=*max_element(v.begin(),v.end());
    for(int i=0;i<4;i++)
    {
        if(v[i]!=m)
        {
          cout << m-v[i] <<" ";
        }
    }
}
